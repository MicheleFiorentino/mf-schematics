{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "custom-lib",
  "title": "Custom Lib",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the library with its inner component",
      "type": "string"
    },
    "project": {
      "description": "Where should be created the library",
      "type": "string",
      "x-prompt": "in which project should the library be created?",
      "default": "projects"
    },
    "style": {
      "description": "The style of the components",
      "type": "string",
      "default": "scss"
    }
  },
  "required": ["name"]
}
