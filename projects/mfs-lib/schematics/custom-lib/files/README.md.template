## <%= classify(name) %>

This library was generated with [Angular CLI](https://github.com/angular/angular-cli) version 15.2.0 and mfs-lib.

## Build

Run `ng build reference-lib` to build the project. The build artifacts will be stored in the `dist/` directory.
